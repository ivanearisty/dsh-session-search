/**
 * Settings namespace owned by this plugin, in its own module so the browser
 * half can import it without pulling the schema (and schemastery) into the
 * client bundle.
 */
export const SEARCH_SETTINGS_NAMESPACE = 'dsh-session-search';
